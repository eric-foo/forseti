# Retail/PDP Storefront Pin Registry v0

```yaml
retrieval_header_version: 1
artifact_role: Current operating registry for Retail/PDP storefront pins
scope: >
  Records the current retailer capture route and independently typed session,
  storefront-country, currency, and delivery-location pin state. It prevents a
  working capture route or observed page context from being misreported as a
  confirmed geographic or fulfillment pin.
use_when:
  - Selecting a known Retail/PDP capture route.
  - Checking which storefront dimensions were actually pinned for a retailer.
  - Locating the latest evidence and re-probe trigger for a retailer route.
authority_boundary: retrieval_only
open_next:
  - forseti/product/spines/capture/core/source_capture_toolbox/capture_recon_index_v0.md
  - forseti/product/spines/capture/core/source_families/retail_pdp/demand_durability_us_storefront_pin_recon_verdict_v0.md
  - docs/research/forseti_beauty_retailer_surface_probe_results_v0.md
stale_if:
  - A listed route, confirmation signal, packet, or retailer surface changes.
  - A later live receipt proves or disproves a pin dimension below.
  - The source-family route home or Source Capture Packet pin contract changes.
```

## Registry semantics

This is the thin current-operating lookup. The capture recon index remains the
historical evidence ledger, and retailer probe result artifacts remain the
claim-level page-state evidence.

Pin dimensions are independent:

- `CONFIRMED_*` requires an explicit retailer signal and a receipt-backed route.
- `OBSERVED_*_UNPINNED` records source-visible context without claiming the
  runner held it fixed.
- `UNKNOWN_NOT_REVERIFIED` means a capture route exists, but this work unit did
  not fresh-read a receipt proving that dimension.
- `UNKNOWN_REQUIRED_SIGNAL_ABSENT` means a live bound probe ran, but the
  commissioned retailer-owned confirmation signal was absent.
- `UNKNOWN_REQUIRED_SIGNAL_SHAPE_MISMATCH` means a live bound probe exposed a
  related retailer-owned value, but not in the exact commissioned shape.
- `UNKNOWN_REQUIRED_ACCESS_BLOCKED` means a live bound probe preserved a
  retailer access-block response before the commissioned signal could be
  observed.
- A product's country of origin, a dollar glyph, a retailer hostname, or a
  working product capture is not a storefront, currency, or delivery pin.

Amazon conditional recovery router: when an Amazon delivery-pinned capture
preserves a final `amazon.sg` or `www.amazon.sg` host, the runner emits
`amazon_us_vpn_fallback_required`. Cold agents must open
`amazon_us_vpn_regression_recovery_playbook_v0.md`; the token authorizes one
Surfshark US / New York retry, not a loop or pin promotion.

## Current operating registry

| Retailer | Current capture route | Session posture | Storefront country | Currency | Delivery location | Last verified evidence | Limits and re-probe trigger |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Amazon US | Fresh anonymous CloakBrowser packet with `--delivery-zip 10001`; the runner requires the requested ZIP in a recognized Amazon location anchor plus an `amazon.com` US-marketplace marker on the final page. `currencyOfPreference=USD` is independent currency evidence, not ZIP proof. Direct network posture remains the first attempt. If that attempt preserves an Amazon Singapore redirect or SG marketplace state, one owner-authorized fallback may connect the pre-existing Surfshark app to its United States / New York route before repeating the same retailer UI flow and admission checks. The widget opener waits once, for at most five seconds, for either known location-anchor selector to hydrate before its bounded click. | Logged-out public; no stored browser profile, cookie injection, capture credential, or login. Default attempt uses no proxy or VPN. The conditional Surfshark retry is allowed only after a typed SG result; preserve the failed pre-VPN packet and record the external VPN posture in the operator receipt without exposing account details. | `CONFIRMED_US` | `CONFIRMED_USD` | `CONFIRMED_ZIP_10001` | Discovery packet `01KXRP77TV6Q2GCG9DM27Z0VTP` confirmed the ZIP-plus-marketplace conjunction on 2026-07-17 UTC / 2026-07-18 Asia/Singapore. LipSoftie `01KXRPBRXVJJ4THQXW63BTV9R2` and SOS `01KXRPF84BA1AZNSVWDACNVSVV` independently preserved Amazon US/USD PDP state, but their historical USD-only confirmation notes do not prove the ZIP. Controlled dogfood on 2026-07-19 Asia/Singapore preserved a no-VPN US/ZIP success (`01KXVB1XCRX211T20EJJ2S8PB8`), an intentionally Surfshark-Singapore SG regression (`01KXVBKECNWG3N43ATMHCVFCGG`), and one Surfshark-US/New York retry that restored Amazon.com but failed the ZIP anchor (`01KXVBT44YJ2JGKZ9JZ5HRD1A5`). Instrumented re-probe measured delayed widget hydration beyond the former 2.5-second cap; the bounded state-wait fix then produced confirmed MakeWaves packet `01KXVF7398CQY2GA4947KW0AAV`, with Amazon header `Deliver to New York 10001`, no access block, and fresh-matched raw hashes. Surfshark disconnect state was verified after both runs. See `amazon_us_vpn_regression_recovery_playbook_v0.md`. | Each future packet repeats the retailer UI flow. A homepage/final marketplace redirect or missing final ZIP conjunction preserves `amazon_delivery_zip_pin_failed` evidence and exits nonzero; do not project or admit that packet as US delivery-pinned. VPN geography is transport posture, not pin evidence: even after the conditional retry, admission still requires Amazon-owned US marketplace, exact `USD`, and delivery ZIP `10001` signals in the final packet. Preserve both pre-VPN failure and post-VPN result. Re-probe and measure if the combined selector does not become visible within five seconds, if the click fails after visibility, or if confirmation anchors or marketplace behavior change. |
| Sephora | Anonymous CloakBrowser with `--sephora-market US` and requested `country_switch=us`; assertion-only confirmation requires `Sephora.renderQueryParams.country=US` plus a Sephora-sold JSON-LD `Offer` with `priceCurrency=USD`. Five-second settle and progressive review scroll remain available where required. | `logged_out_public`; no profile, storage state, proxy, VPN, geo-IP, cookie injection, credential, or login. | `CONFIRMED_US` | `CONFIRMED_USD` | `UNPINNED` | Tower 28 LipSoftie packet `01KXRZQJBKNKC91SXH2C7MKF1C`, captured 2026-07-17 UTC / 2026-07-18 Asia/Singapore with `pin_confirmed=true`; all four raw hashes and the availability-index manifest hash fresh-verified. | The URL switch is request intent, not proof; the rendered conjunction and final Sephora host decide admission. A missing or contradictory conjunction preserves `sephora_market_pin_failed` and exits nonzero. No delivery destination was established. |
| Ulta | Anonymous CloakBrowser on the canonical Night Shift PDP with `--ulta-market US`, five-second settle, one scroll pass, and explicit subject/SKU/price/review sufficiency checks. The assertion performs no mutation and requires consistent root `lang=en-US`, `window.__APP_LOCALE__='en-US'`, and Ulta GraphQL `ultasite=en-us`; one product price node binding `data-consumer-locale=en_US`, `data-currency=USD`, and a nonempty amount; plus the exact requested SKU's Product JSON-LD nonempty USD offer. `ulta_pdp_aggregate` defaults to canonical content after those gates pass. | `logged_out_public`; no profile, storage state, proxy, VPN, geo-IP, cookie injection, credential, login, cart, or delivery mutation. | `CONFIRMED_US` | `CONFIRMED_USD` | `UNPINNED` | Fresh no-VPN Night Shift packets `01KY07RNVTEN70R3BYQKNK0DW0` (content) and `01KY07SM8Y3S2BBAXD789GC1W1` (raw), captured 2026-07-20 UTC, both preserved `pin_confirmed=true`, the exact requested/final `www.ulta.com` PDP, SKU `2645443`, price `12.00` USD, in-stock state, rating `4.3` across `671` reviews, and five target Product JSON-LD review bodies. Cleaning-selected offer/review rows and Retail Silver payloads were equal apart from lineage. The complete content packet measured 201,117 bytes versus 1,772,930 raw bytes; its 8,366-byte content record replaced 1,580,525 bytes of disposable DOM/text while retaining the viewport screenshot. Earlier admitted packet `01KXWQW45J2C17RX4397Z27KYJ` and failed-signal packet `01KXSSJBMN6T4Z480ZX1RHA3MB` remain historical evidence. | The prior rule checked nonexistent `window.__LOCALE__` and `data-locale` names; the preserved page actually exposed the first-party `__APP_LOCALE__` and `data-consumer-locale` fields used by the admitted route. The stale visible-text sufficiency literal `Offers` was replaced by the current exact `In stock and ready to ship` wording. `.com`, absence of `.sg`, a dollar glyph, HTML language alone, or VPN geography cannot substitute for the full retailer-owned conjunction. Because the direct route proved the complete conjunction, the conditional VPN comparison did not trigger. A missing/contradictory signal, host/SKU drift, or extraction failure preserves supplied raw evidence and exits nonzero. Full Apollo/loader state is not retained as content; its input hash remains recorded. No delivery destination was established. |
| Walmart | Anonymous Direct HTTP PDP with `--walmart-market US` and `walmart_pdp_aggregate`. The post-fetch assertion requires one `props.pageProps.initialData.data` object to bind the URL item to `product.usItemId`, exact current-price `currencyUnit=USD`, equal nonempty page/product postal context, and an immediate `contentLayout.modules[*]` or `contentLayout.lazyModules[*]` targeting object whose `countryCode` is scalar `US` or Walmart's exact single-item `["US"]` serialization. | `logged_out_public`; explicit no-proxy opener, with no VPN, cookie injection, profile, credential, login, preference mutation, or delivery mutation. | `CONFIRMED_US` | `CONFIRMED_USD` | `OBSERVED_LOCATION_UNPINNED` | Vitamasques packet `01KXWY75J4419N85NAXPXGZG8Q`, captured 2026-07-19 UTC with HTTP 200, `pin_confirmed=true`, exact requested/final `www.walmart.com` item `2150828728`, exact slice pins `US` / `USD`, and both raw hashes and byte lengths fresh-verified. Derived projection `01KXWY7K99EPG92XW2GZ6BTTH0` anchored one product row and one offer row for item `2150828728`, price `2.97`, currency `USD`. Earlier packet `01KXSV9HFFEPNEXVA407318KW1` remains historical evidence of the disproven scalar-only rule. | This is an assertion over retailer-owned current page state, not a location setter. Postal `95829` is origin-derived context and remains unpinned; the projection explicitly retains absent exact inventory quantity, sold units, delivery-location pin, and review substrate. `.com`, a dollar glyph, broad list membership, or proxy/VPN geography cannot substitute for the full conjunction. A missing/contradictory signal, country list containing any value other than the single `US`, host/item drift, or projection gap preserves typed evidence and promotes no pin. |
| Target | Anonymous CloakBrowser with `--target-zip 10001`, six-second settle, one scroll pass, and subject-specific sufficiency gates. The plugin navigates the exact commissioned Target surface, waits once for the public `#zip-code-id-btn` to become visible, fills the visible ZIP input scoped to Target's dialog/ZipCode component, and applies it through the visible action or the already-scoped input's Enter form submission. Admission requires completed UI setup, exact header `Ship to location: 10001`, exact child `Ship to 10001`, and `serverLocationVariables.location.country=US`. | `logged_out_public`; anonymous ephemeral session with no profile, storage state, proxy, VPN, geo-IP, cookie injection, credential, login, or cart action. | `CONFIRMED_US` | `OBSERVED_USD_UNPINNED` | `CONFIRMED_ZIP_10001` | Naturium grid `01KXWMQWHRABBSA5HN3CMR9TNS` and PDP `01KXWMXEYB58SMVSJT80XJAP9D`, captured 2026-07-19 UTC with `pin_confirmed=true`, exact requested/final `www.target.com` URLs, no access block, and all eight raw hashes and byte lengths fresh-verified. Grid and PDP state bound shipping/server ZIP `10001`, country `US`, and separate Chelsea 23rd and 8th store ZIP `10011`. Earlier failed timing/control packets remain historical evidence. | Shipping ZIP and store/pickup ZIP remain independently labelled concepts; store ZIP `10011` is not reported as delivery ZIP. `.com`, absence of `.sg`, a dollar glyph, or VPN geography cannot substitute for the retailer-owned conjunction. Target exposed no admitted explicit currency-code conjunction, so currency remains observed but unpinned. Re-probe if the public control does not become visible within the bounded readiness wait, scoped dialog input/action semantics drift, final shipping signals disagree, or the Target-owned country signal changes. |
| Luckyscent | Direct HTTP for the brand grid remains raw. Anonymous CloakBrowser `luckyscent_pdp_aggregate` uses `--luckyscent-market US`, five-second settle, 500-pixel scroll steps, and four passes; it defaults to retailer-owned content mode after pin, access, sufficiency, and extraction pass. The flag performs no market mutation: it confirms one serialized storefront `i18n` object binds `country=US`, `market=market-us`, and `currency=USD`. Before snapshot, one route-owned action may close only the exact Klaviyo first-order promotional modal through its exact `Close dialog` control; changed or unresolved modal state preserves raw evidence and fails admission. | `logged_out_public`; no profile, storage state, proxy, geo-IP, cookie injection, credential, or login. | `CONFIRMED_US` | `CONFIRMED_USD` | `UNPINNED` | Bread and Roses US-market verification `01KXRG2C722GPTCVF6V8MFR4Y5`, captured 2026-07-17 UTC / 2026-07-18 Asia/Singapore with `pin_confirmed=true`; post-cut isolated content canary `01KXZV2VDX31YPFD25N30P42TT`, captured 2026-07-20 UTC, also passed with `pin_confirmed=true`, an unobstructed viewport, three offers, and all eight reviews. Earlier grid `01KXRDAWN0DC727R66HMDDYJ2D` and PDP `01KXRDEEQX391STPRWH21RTSMA`. | Luckyscent exposes no country selector; the confirmed pin is its canonical default storefront market, not a mutated preference. A missing or contradictory conjunction preserves `luckyscent_market_pin_failed` and exits nonzero. The named overlay action never submits email, grants cookie consent, or handles a challenge; `luckyscent_overlay_dismissal_failed` preserves supplied raw evidence and exits nonzero. Content mode accepts the source's current single ProductGroup or earlier redundant ProductGroup+Product structured shape only when the same required product/offer/review substrate is present. It retains the target's three variants and eight rendered reviews, hashes then discards DOM/text, and keeps the viewport screenshot as active-capture evidence. `buyerCountry=SG` remains a separate origin-derived shopper signal, and no US delivery destination was established. Re-probe if the loader context structure or values change, `pin_confirmed` becomes false, the named modal changes, or either route stops binding the commissioned subject. |
| Nordstrom | Anonymous CloakBrowser with `--retail-capture-profile nordstrom_pdp_aggregate --nordstrom-country US`; the setup first tries Nordstrom's homepage country control and, if that control is absent, the same retailer-owned control on the exact commissioned PDP. Retailer state must finally yield selected US/USD plus shopper context `CountryCode=US`, `CurrencyCode=USD`, `IsInternationalShopping=false`. The aggregate PDP admits canonical content only after pin, access, sufficiency, and extraction succeed. For bounded onboarding, add `--nordstrom-review-posture recent_window_30d`: retain the source-labelled most-helpful positive/critical pair, select `Most Recent`, retain the complete 30-day cohort, and when it has fewer than 12 reviews continue to 30 Most Recent rows or proven source exhaustion. | `logged_out_public`; no profile, storage state, proxy, geo-IP, cookie injection, or credential. | `CONFIRMED_US` | `CONFIRMED_USD` | `UNPINNED` | Fresh content-v2 evidence `01KY0352HY3T6ZG82BZTBC9TEG`, captured 2026-07-20 with `pin_confirmed=true`, content extraction succeeded, 30 Most Recent rows, four continuations, zero rows inside the 30-day window, and `historical_context_complete`. Hash-verified sampled-raw evidence `01KXY400VV93RSGFSE9ZSXJ50Y` remains the exact-byte parser-fit anchor; typed raw failure `01KXY3VH1M543DPW5984HSJH6Q` remains preserved. Earlier PDP `01KXRBDXGG092NXVRKJWJFB5JB` and grid `01KXRBHKAA6D64K31BY54JKGJN` remain historical 2026-07-17 evidence. | The target-page fallback does not relax admission: final rendered Nordstrom state remains authoritative. PDP still displayed `Shipping to 518225`. The content record preserves that exact display as `nordstrom_shipping_destination_display_is_not_delivery_pin`; it is never US-delivery evidence. Pin, access, sufficiency, extraction, or requested review-posture failure preserves raw inputs and exits nonzero. `Load 6 more reviews` is one action that adds six further rows; the initial six are not themselves a pagination action. Older fallback rows record `historical_context_complete`; a 30-row cap hit before the recent window closes records `recent_window_truncated`. Re-probe if either country control disappears or country, shopper-context, target Product JSON-LD, target initial product state, review-sort, highlighted-pair, main-card, date, or continuation semantics change. |
| Credo | Anonymous Direct HTTP PDP with `--credo-market US`. The assertion requires the exact `credobeauty.com/products/<handle>` requested/final route and canonical URL, only `Shopify.country=US`, only active `Shopify.currency=USD`, and exactly one route-bound Product JSON-LD object with a named brand and at least one nonempty priced USD offer; mixed or split country/currency/offer state fails closed. | `logged_out_public`; explicit no-proxy opener, with no stored profile, cookie injection, credential, login, cart, preference, or delivery mutation. A post-capture adapter/route check found both Surfshark adapters disconnected and the default route on Ethernet. | `CONFIRMED_US` | `CONFIRMED_USD` | `UNPINNED` | Tower 28 SOS Daily Rescue Facial Spray packet `01KXX1S3VYWQM3NNM23VYK3EPW`, captured 2026-07-19 UTC with HTTP 200, `pin_confirmed=true`, exact requested/final/canonical Credo PDP, Shopify `US` / `USD`, and a bound Tower 28 Product with USD offers. Both raw hashes and byte lengths fresh-matched. Corrected derived projection `01KXX3P3PPQV3625JKFDGA3JY7` anchors selected SKU `210000007835` at `12.0` USD plus Yotpo aggregate `4.705247/5` from 648 reviews and exactly 10 displayed review bodies. Bounded review packet `01KXX5M6BGQ6CM9XJ1752F7N2Y` and coverage-v1 projection `01KXX6RCN8C2XTH3JZF055RMSJ` separately preserve rating-first/date-sorted response state, including incentivized and reviewer-declared age-range fields. | This confirms Credo's current default storefront state without activating a VPN; it does not establish a US delivery destination. Both 10-body views are bounded subsets, not the 648-review corpus; the review packet's `fragrance_review` family name is a typed legacy naming limitation. `.com`, absence of a country suffix, a dollar glyph, Shopify response cookies, or network geography cannot substitute for the full retailer-owned body conjunction. Re-probe if the canonical/product route, Shopify globals, Product URL/brand, offer currencies, exact Yotpo section, or Yotpo response-field shape changes. |
| Kohl's | **Admitted unattended route:** `run_kohls_unattended_capture.py` starts a temporary headful Google Chrome under Xvfb, attaches through the rung-7 `run_source_capture_realchrome_cdp_packet.py` seam, captures the bound PDP and policy independently, then stops and removes its container. The image is built only when missing; a private Docker volume preserves the Chrome profile. The CDP port is assigned dynamically and bound only to host loopback. The generic rung-7 runner still supports an operator-provided real Chrome for other targets. **Headless armory routes remain NO_GO** on Kohl's: Direct HTTP, header-complete HTTP, real-Chrome headless, and CloakBrowser attempts were denied. | `logged_out_public` unattended real Chrome via private CDP; no login, cookie injection, credential, cart, or delivery mutation. The Docker profile volume persists browser state, but packets preserve only public page content—not cookies, browser chrome, or account state. Homepage warm hop before each target. The live Windows Docker Desktop proof required Chrome's inner sandbox to be disabled while Docker remained the outer isolation boundary. US context is retailer-owned, not viewer geography. | `CONFIRMED_US` | `CONFIRMED_USD` | `UNPINNED` | Unattended GO packets PDP `01KXXV920Z8PQVHP6DN16335DF` and policy `01KXXVA8B5EHY86N4EJZ7Y6360`, captured 2026-07-20 (`source_surface=realchrome_cdp_snapshot`, HTTP 200, no access block, exact sufficiency gates passed, `browser_provisioning=unattended_xvfb`, `persistent_profile_loaded=true`). PDP raw DOM binds Tower 28 LipSoftie and schema.org Offer `priceCurrency=USD`; the policy packet binds Kohl's own US/APO/FPO-only shipping statement. Earlier operator-provided rung-7 packets `01KXXK9PJTA2KBRS2CM1MZDV1H` / `01KXXKA9RJBP6SVT455DTR0K08` and hand-bridged packets `01KXXHBKF2GPK4M96SAV1VQKM3` / `01KXXHC3BZ27D3WWDJ8QPV8004` corroborate. Historical block packets remain indexed in the retailer probe result. The generic Retail/PDP projector still reports `variant_offer_absent` because it parses JSON-LD, not Kohl's microdata offers—a projector-coverage residual, not an absence. | The pin rests on retailer-owned Offer `priceCurrency=USD` plus Kohl's own US-shipping-only policy—not `.com`, a dollar glyph, search text, or proxy geography. Delivery remains UNPINNED because no US delivery address was established. The route passed even though the proof browser exposed no usable WebGL context, so the earlier “real GPU required” explanation is retracted; the exact Akamai discriminator is not isolated. No recurring frequency is registered: each one-shot writes two durable packets. Re-probe if the Offer microdata, policy wording, subject binding, Chrome/container route, or Akamai behavior changes. Linux sandbox behavior and arbitrary datacenter egress remain unproven. |

## Non-claims

This registry does not establish inventory depth, fulfillment availability,
realized transaction price, demand, velocity, revenue, sell-through, market
share, customer sentiment, monitoring readiness, or commercial readiness.
