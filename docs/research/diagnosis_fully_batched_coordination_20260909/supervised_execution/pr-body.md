The fixed-evidence experiment tests bounded diagnosis, source-backed verification and the cost of coordinating those calls.

The paired verification follow-up preserves nine fresh diagnoses, a separate historical positive control, exact corrections and isolated paired judgments. Fresh material errors fell from five to two; the known-error control was corrected separately. Surviving material errors remain visible.

The execution follow-up adds a sample-local entrypoint for the frozen positive-control verifier. Code handles process supervision, failure logs, one expected-duration review, correction application and native accounting. Dogfood preserved exit 7 and ENOENT, allowed an overdue process to finish without restart, and completed a controlled 70-second quiet process with one outer follow-up wait. The real verifier used one call and 38,928 tokens, correcting the known congestion error.

Internal supervision makes no model calls. The mandatory 60-second update instruction remains, and the long outer authoring conversation remains separately charged: 976,925 tokens through its explicit cutoff, with later reporting/publication additional. No reduction in full-experiment cost or fresh-context coordinator cost is claimed.

Validation: frozen input/implementation hashes, completed native accounting, exact correction replay, first-pass outputs, failure and quiet-job checks, and scoped durable artifact readback. Reports and the executable entrypoint are linked from docs/research/README.md.

Research only; no production rollout or global runtime changes. Different-vendor operator-courier review and author adjudication remain required before merge. Keep this PR draft; no merge or automatic continuation.
