# Bounded complete-case consumer implementation

```yaml
retrieval_header_version: 1
artifact_role: Bounded implementation and validation record
scope: Complete-case consumer continuation, observed tests, and remaining fit limits.
use_when:
  - Reviewing or continuing the bounded consumer implementation.
authority_boundary: retrieval_only
```

This implementation record is for the owner and the next executor. Authority is
the 2026-09-22 bounded consumer commission, the semantic integration contract,
and the claim-support contract. Starting revision: `a9384f6aa323f1d703cd82eca5d7bc8c20115c47`.

## Success contract (bound before edits)

Goal: consume the normal policy-v2 completed case into source-backed findings,
answers, and review without putting the whole raw corpus into every request.
Existing extraction and verification remain authoritative for their own scope;
they do not establish downstream relevance or correct interpretation.

Invariants: exact complete inventory, separate support/opposition and original
conditions/roles, explicit unused/residual explanations, no silent truncation,
local content-bound reuse, original hashes and finite replay unchanged.
Acquisition, corpus repair, new consolidation, and global finite-route adoption
are outside this change.

Signals, initially `PLANNED_NOT_OBSERVED`:

- Public normal advance with a commission reaches source judgments, assembly,
  review, and a persisted answer. Split support/opposition and an important
  unused observation remain available together; harmless no-action remains
  explicit. Wrong-cause perturbation: omit an admitted unit from a valid-shaped
  response and require exact-coverage rejection.
- Every production request includes schema, context, and output reservations
  in capacity admission. An oversized row, finding slice, aggregate answer, or
  review must block before dispatch; no partial success is allowed.
- Missing, duplicate, and foreign results fail at the consumer boundary.
  Repeated valid requests reuse accepted work; evidence, checks, and answers
  invalidate only dependent work. Historical finite artifacts stay replayable.
- Structural fixtures do not establish semantic quality. A cold live case must
  discriminate contrary evidence, material omission, uncertainty, and harmless
  residual/no-action preservation, with expectations excluded from actor input.

Baseline: normal `advance` ends at `view.json`; no commissioned bounded answer
surface exists. Parent separately preserved 50 passing unchanged review-evidence
and finite-closeout-judgment tests. This is an absent-surface baseline, not a
fabricated red test.

Review checkpoint: apply the repository's conditional success-implement review
predicate. Any material boundary lacking independent evidence receives a
bounded operator-courier review prompt; no reviewer dispatch in this unit.

## Integration success contract (bound before integration edits)

One supported normal `advance` command can begin with an explicitly paired,
native-validated existing bundle and verified compilation for the same source,
retain that start across resume, run unchanged policy-v2 reconciliation, then
deliver bounded consumer jobs through `intake-judgment-job` and
`submit-judgment-job`. Complete/selected verification retains original provenance;
missing or changed originals and a mismatched source block before judgment.

New answer reviews explicitly classify repair scope and may propose exact edits
under the existing `review_evidence` policy. One admissible patch receives one
independent recheck and existing clean-only selection; no inferred classification,
extra rewrite, automatic retry, or source reread. Saved unclassified reviews
retain their failure. Publication shares staged no-replace mechanics and resumes
a valid response missing its receipt without creating a new judgment.

Bound validation is structural public-CLI replay from genuinely native-verified
synthetic inputs, established intake/submit, exact correction/recheck, no-work
resume, provenance perturbations and interrupted publication. No provider calls
or Summer Fridays processing; prior live evidence stays historically pinned,
full current-case fit remains unproven, specialized point readers stay unchanged.

## Implementation

The public normal `advance` entry now accepts an answer commission and explicit
capacity settings. It reaches source review, cross-batch assembly, independent
answer review, bounded original-source reopening, and immutable result publication.
The native reconciliation route and existing finite/replay implementations are
unchanged. The semantic-integration contract's **Bounded complete-case answer and
review** section owns the executable interface.

Existing verified meanings, residuals, and reasoned no-unit dispositions are
reused. Source review covers the claim-bearing rows and explicit check anchors;
it does not reclassify every no-unit row. Every distinct unused/no-claim reason
remains available to final actors. Exact source membership stays in immutable
manifests; compact handles and zero-based origin-attribution indices preserve
the same records without repeatedly printing long source identities. Known
equal/distinct origin identity, roles, and independence posture cross batches;
unknown identity is not minted into a new person. The established worker intake delivers only the measured prompt/schema envelope;
required context is already present once, and the duplicated local-validation
payload is not redelivered.

Content-addressed requests preserve accepted work, reject changed accepted
responses, and stop on a missing response whose acceptance receipt exists.
Changing checks reuses unchanged source judgments/assembly. Changing a source
row invalidates its slice and downstream consumers. Reviews bind actual answers.
New reviews explicitly classify findings/check scope and may supply hash-bound
exact answer repairs. Existing `review_evidence` application, composition and
clean-only selection connect one repair to one independent recheck. Inadmissible,
upstream-only, unclassified and failed corrections preserve the original and
visible failure. This does not claim the optional finite v4 improvement rule.
Unchanged source jobs and answers remain exact; no extra rewrite or retry runs.

## Historical initial validation

- Fourteen focused consumer tests passed and exercise the public normal CLI end-to-end, complete
  split opposition and residual coverage, harmless no-claim reuse, exact
  response and compiled membership, capacity/output failures, original-source
  reopening, local reuse/invalidation, repeated/distinct/unknown identities,
  lossless transport partition, measured delivery, and accepted-response loss.
  The final focused run is retained as `final_consumer_tests.txt` in the evidence
  root below. A valid-shaped missing/duplicate response fails the intended local
  coverage boundary; a seeded omission of the opposing compiled member fails
  `consumer compiled membership`.
- The unchanged finite runner and finite-closeout suites passed: 128 + 48 tests.
  Normal advance plus the selected coupling/inventory/version-pin tests passed
  43 tests. Parent separately preserved the 50-test review-evidence and
  finite-closeout-judgment baseline at the starting revision.
- `git diff --check` passed. The diff-based coupling adapter skipped because
  this delegated unit is uncommitted; its actual contract files were exercised
  directly. Full CI, publication, review execution, and merge were not run.

Evidence root (run-local, preserved):
`C:/Users/vmon7/.codex/tmp/forseti-complete-sf-comparison-20260922/consumer-implementation/`.
This is the next source for exact request bytes, responses, receipts, and sizing
scripts; do not reconstruct them from this narrative.

## Synthetic semantic quality and observed cost

Three parent-authored expectations were frozen before model outputs. Eight
synthetic statements tested: benefit versus actual discontinuation in different
source jobs; a residual mitigation observation outside the retained old-failure
finding; and intent-only evidence that warrants no product action. Expectations
were excluded from actor prompts. This is not Summer Fridays processing.

The final answer preserves all three distinctions, and the independent final
review passes all three. A deliberately faulty answer was independently rejected
for all three specific errors: omitted adverse experience, old-to-current
failure generalization with omitted mitigation, and invented switching/causation.
Parent independently evaluated the saved answer/review against its frozen oracle:

- `parent_quality_evaluation.json`, SHA256
  `fc0d87ca824c2b9a90dc06821b01d370feab09e5b875a1c9da42d5beab361a90`.
- Positive result:
  `live/consumer/results/f631cbae44d329d3ad4794af0854888d5e8bd9fc91a6e8d078b61ebf8784e532.json`.
- Negative review:
  `live/negative/6279ad459fe6a02d485df6c351da4b2edc75fb305c5d445af512810fe8f3f194/response.json`.

Fifteen technical attempts are retained: 11 completed, four rejected by the
provider's unsupported `uniqueItems` schema constraint (usage unobserved).
The schema was corrected while local uniqueness/coverage enforcement remained.
Other diagnosed defects were preserved: overly strict citation-incidence
counting, the first fixture's unsplit opposition, missing final origin identity,
and an invented uncommissioned review check. Their bounded corrections and
targeted reruns did not erase the original responses. No open-ended semantic
repair loop ran.

`live_usage.json` records 316,466 observed input tokens (including 15,616 cached)
and 6,043 output tokens (including 3,244 reasoning). Subsets are not added twice.
No billed dollar value was observed. Actual provider input exceeded local o200k
prompt/schema counts by 18,153–18,260 tokens. Final historical planning therefore
uses 20,000 other-overhead tokens, not the initial provisional 2,000. These
observations establish neither savings against a matched control nor model-tokenizer
equivalence. Summer Fridays provider processing in this unit was zero.

The live final pair used string origin aliases. The subsequent numeric-origin
and finding-position transport change is covered by exact identity/relationship
and delivery tests, not relabeled as a new live semantic run.

## Historical scale and remaining limits

`offline_final_calibrated_sizing.json` uses production request builders and the
unchanged historical method-v7 customer-only source: 60,901 captured rows, 3,339
source-review rows, 57,562 reused no-unit/mechanical rows, 8,020 verified meanings,
107 retained findings, and 7,700 residual meanings. It preserves all 346 distinct
no-claim reason groups and 2,041 known origin identities. The historical v2 view
was measured without restamping it as current execution evidence.

The generated-content stress shape copies each historical verified statement
into one putatively material finding. It is explicitly sizing-only: no source
review judgment or semantic relevance was fabricated. Sixty-seven source
requests fit the declared planning budget; the largest reserves 105,000 tokens.
Final assembly measures 217,733 input/schema tokens and review 217,941. Adding
8,000 output and 20,000 other-overhead reserves gives 245,733 and 245,941. Against
258,400, then allowing another 12,000 for actual answer/reopening content, margins
are only **667 and 459 tokens**. This narrow historical margin is not robust
execution-fit evidence. Earlier 197k or 246k figures used different transport or
insufficient overhead and are superseded for planning by the calibrated record.

These historical sizing counts also predate the normal worker-handoff accounting
and explicit repair/recheck schema added by the integration below. They are not
current request-fit measurements; their narrow margins cannot be carried forward.

The current corrected, multi-family Summer Fridays case, actual generated
findings/answers/reopening volume, and tokenizer/model equivalence remain
unproven. The original full-case execution-fit blocker is **not fully cleared**.
The machinery now fails visibly instead of sampling or truncating when any real
downstream request exceeds its bound. Corpus repair and full product binding
remain separate pending work; this change neither shrinks that case nor silently
uses the historical customer-only source as complete current evidence.

## Integration validation and corrected boundaries

The normal command now supports a paired `--bundle` / `--verified` start,
validated by a shared native source/verification validator also used by row
selection. Every resume validates the selected originals and immutable start.
Method-v7 selected evidence reaches normal reconciliation with native schema
metadata; historical prompt records and method/policy versions remain unchanged.

The parent subprocess proof exposed JSON object ordering as a real restart
invalidator: equal native evidence regenerated different prompt hashes and
reissued source judgments. Canonical object rendering fixes this without
reordering arrays. Previously saved transport variants are reused only after
exact instruction/context prefix, losslessly expanded evidence, schema, capacity
and actual measurement revalidation. Conflicting accepted judgments fail
explicitly. The lookup exists only in memory within one advance.

Structural tests now cover genuine prior native verification through public
normal advance, standard intake/submit, exact correction/recheck and no-work
resume; unchanged second answers and source responses; failed correction
preservation; explicit upstream/unknown classifications; legacy review reuse;
response-before-receipt interruption; partial staging refusal; separate-process
hash-seed stability; and conflicting historical transport variants. Wrong-source,
coherently changed original proof and missing original selection dependencies
block before new judgments. These are structural tests, not new semantic-quality
or full-case-fit evidence.

Final integration evidence is under the earlier evidence root's
`coherent-integration/` directory. The initially failing broad run is retained as
`integration_tests.txt`: 826 passed and three failures diagnosed as a legacy
review ordering mismatch and native historical prompt-metadata drift. The final
run is `integration_tests_final.txt`: **831 passed in 127.10 seconds**, covering
consumer, verification selection, the complete native semantic suite, shared
review helpers, finite execution/closeout, and inventory/version-pin contracts.
The final handoff-accounting-only edit then passed **24 focused consumer tests**,
including a core-envelope-fit / worker-handoff-overflow discriminator; that run
is retained as `integration_consumer_delivery_final.txt`.

Parent independently exercised actual public CLI subprocesses with the cached
o200k tokenizer, current project context, and deterministic fixture responses:
`parent-public-after-v4/result.json`, SHA256
`6783b298ad2fbaca5bd1a46fce99aebff6e8f6bb756a9974771a3cde6fc4e2ff`.
The sequence was two native reconciliation jobs, four source reviews, assembly,
answer review, one exact repair/recheck and `COMPLETE_CASE_ANSWER_CHECKED`.
There were no extraction/verifier jobs; the native view stayed exact, second
answer/source bytes stayed unchanged, response-before-receipt recovery succeeded,
and completed repeat issued zero jobs. Changed source/verification bytes blocked;
restoration resumed the same answer. Parent used the already available tokenizer
at the existing `step2/tools` location through scoped `PYTHONPATH`; no dependency
was installed. The later handoff-accounting change has separate focused evidence.

Parent also resumed the preserved pre-fix v3 run with 16 prior source-request
ordering variants: zero new source jobs, all prior source bytes preserved, and
only assembly requested under current provenance. Evidence is
`parent_legacy_reuse_result.json`, SHA256
`510295b7ab1e91cbcba4302b9575be39527c98e343f6f5690c10b66e9f38a853`.
The original discovered failure remains at `parent-public-after-v3/failure.json`,
SHA256 `3d761715fd080f8d32078278d80cbac5d42c793643e7b1fe04f61c57f1b3a067`.

Parent's first changed-handoff check passed before the later chunk transport edit:
`parent_final_handoff_check.json`, SHA256
`07f9c31ddd37819ddea1b9426e027e082edaf3d1ac0e1aee8dcfac3cc7469ca5`.
A completed public resume issued zero jobs and preserved exact answer bytes.
A 38,872-token declared budget fit the core envelope but the measured worker
handoff required 40,862; the CLI blocked at that boundary with zero judgment
requests. Restoring capacity resumed the same accepted answer. Offline tokenizer
and runtime-reserve uncertainty remain explicit; this is not a full-case fit claim.

The parent then exposed a distinct transport failure with an otherwise fitting
large synthetic request: capturing the entire intake under the tool's 60,000-token
stdout allowance truncated it before the original script could split the text.
The final `intake_end` survived, but JSON parsing failed. The original probe and
failure remain under `large-delivery-probe/`. A temporary conservative byte-ceiling
experiment (`transport-overflow/boundary.json`) was rejected because it would
block otherwise fitting work; that limit is not the final implementation.

Large consumer delivery now selects bounded retrieval automatically before stdout
capture, through optional manifest/section/byte-offset arguments on the existing
intake command. Each return revalidates the same job, carries at most 8,000 UTF-8
bytes with complete codepoints, and includes chunk/section hashes. The generated
script checks all identities, offsets, byte counts, individual chunk SHA256 and
full-section SHA256 before emitting final completion. Existing default intake
and native worker generation remain unchanged. Known selected-transport framing,
end markers and hashes are included in capacity accounting. Sequential chunk
retrieval adds latency for large requests; no batching optimization is claimed.
The implementation does not add a registry, acquisition, provider calls or a
second judgment policy.

Chunk tests include Unicode boundaries, actual large CLI serialization, full
reconstruction against independently computed hashes, parseable truncation,
missing chunks, changed jobs and same-length corruption. The initial chunk
regression run exposed three native-generator compatibility failures; all were
fixed by keeping native generation pure and passing actual measured intake size
only from the consumer. Its failed log remains `integration_chunk_delivery_final.txt`;
current focused evidence is `integration_chunk_delivery_verified.txt`: **75 passed**
across all consumer tests and the affected native advance/worker boundaries.

Parent executed the exact generated large script through real nested tools,
without sending its evidence payload back to a model judgment. All 112 inner
commands completed, delivering 111 chunks with a maximum returned length of
8,493 characters and no truncation. Final prompt/schema byte offsets were
879,417 and 406; reconstructed SHA256 values matched the independently saved
complete intake. Evidence: `large-delivery-probe/actual_tool_delivery_result.json`,
SHA256 `c77fe370c1f2f32bbb1d4fa29e0d68530d04bfc510ccf5a8a4688c6e7464ec79`.
The exact exercised script SHA256 is
`b480df340217de2a2cc40c6029c7f46012323b2675c8b0ea4834eea02881e42a`.
This proves that fixture's complete tool delivery, not semantic judgment or
current full Summer Fridays execution fit.

The final public resume/capacity check on the chunk/framing revision also passed:
`parent_final_handoff_check_v2.json`, SHA256
`f72cc3a66b5d0d05dd664a204f8e213bc87b5d0af2a60e9db8ec2119b5934b48`.
Completed repeat issued zero jobs and retained exact accepted answer bytes. The
38,872-token core-only budget blocked the measured 40,945-token worker delivery
before dispatch; restored capacity resumed the same answer. These final source
pins matched the implementation-validation courier and final delivery receipt.
That receipt remains the pre-freeze validation snapshot; the later review courier
binds the frozen implementation commit instead of mutable file hashes.


At implementation validation, direct scans of the four changed durable Markdown
targets found no handoff or
ontology-tag findings. The courier passed output-mode validation, and its six
raw-file code/test pins were freshly verified against their durable targets.
Diff-scoped document hooks selected zero files on this uncommitted work unit;
they are not substituted for those direct checks. `git diff --check` passed.
Required CI and courier review remain unexecuted.

No provider jobs or Summer Fridays processing were run in this integration.

## Review disposition

The Anthropic controller returned review of immutable revision
`88ad6fe8463db000c9101ad2f6ed9bb4cf514cf4` with changes limited to the consumer
and its unit tests. Home-model adjudication kept both submitted fixes:

- F1 accepted: submit must not publish a later judgment when an accepted response
  is missing but its receipt survives. The same restore-only failure already
  governs advance; the patch now enforces it before response publication.
- F2 accepted: schema-invalid responses must reach the existing structured error
  boundary. Preserve the schema failure's explanation through `ValueError`;
  rejected responses remain unpublished.
- R1 accepted and closed in the existing consumer reuse boundary. Compare exact
  context bodies and verified hashes under the existing roster's logical file
  names, allowing only checkout-location header changes. Preserve original
  request/answer/receipt bytes and measured delivery. Changed logical files or
  contents do not reuse; forged context hashes and conflicting accepted judgments
  fail. No shared provider launcher change or new persisted identity registry is
  needed. Input/start bindings remain exact.
- R2 accepted and closed: validate nonempty string encoding before advance's
  tokenizer lookup, and validate saved consumer capacity at request intake and
  both public submission commands. Missing/empty encoding returns structured
  failure with no response publication or new judgment request.

The relocation regression failed before R1's patch by returning new source jobs
instead of the already checked answer. After the patch, the public route copies
all four actual context sources to another checkout and returns the exact same
answer with zero jobs and every consumer JSON artifact byte-for-byte unchanged.
Additional negative cases cover changed source contents, changed logical names,
forged hashes, conflicting accepted judgments, and literal END SOURCE text inside
a context file. Missing encoding reproduced an uncaught KeyError on advance and
the compatibility submit command before their corrections. The test initially
expected exit 1; it was corrected to the existing runner's documented-in-code
error exit 2, while retaining the structured-block/no-publication assertions.

Parent validation on the final patch: 97 consumer/selection/review-evidence tests
passed; 64 native advance, judgment-delivery, inventory and version-pin tests
passed (510 deselected). The controller's original 91 + 64 results remain its
reported evidence; the parent independently ran both suites on the adjudicated
working tree. These tests are structural, not model-quality proof.

The saved independent public CLI fixture was also rebuilt from its bound inputs
with the real offline tokenizer and deterministic responses. All nine phases
passed; the answer file exactly reproduced pinned SHA256
`fb1a0991138dddd9adf0833d55ed175073953d86c6f0f23f2979461475338d38`, completed
resume issued zero jobs, exact correction/recheck retained originals, and changed
input bindings remained blocked. Working-tree evidence:
`C:/Users/vmon7/AppData/Local/Temp/forseti-consumer-adjudicated-gu_2r25i/public/result.json`,
SHA256 `dc7cb1a525cd954cd79c5ed599d1f9ac1f65cf126051949b5d06058df46149cd`.
The first scratch replay was blocked by sandbox access to the existing optional
tokenizer; a second attempt could not read the first sandbox's scratch file. The
successful run read the original script and bound inputs directly with approved
access. No dependency was installed or input evidence changed.

The controller's clean-export replay preserved semantics but changed request IDs
with checkout location; its patched-original-path replay preserved the answer
bytes. Its legacy-reuse reproduction preserved prior artifacts. It did not
reproduce the live nested-tool delivery pin or locate the final handoff replay
script. Those two pins remain historical observations, not fresh controller
proof. This adjudication changes response admission/context reuse, not chunk
transport or generated framing; the passing scoped suite covers those existing
boundaries. No provider jobs or current Summer Fridays processing were run.
Full-case input readiness, full-case capacity fit and semantic quality remain
unclaimed. Writer isolation was observed from clean intake/no lock and the
returned bounded diff; this is not proof of a concurrent-writer capability.

The transport-only courier is retired in this change after adjudication; its
exact review instructions remain in Git history at the reviewed revision. This
section owns the current disposition, and the PR owns landing/CI state. No
unresolved material review finding remains.

review_routing_status: routed -- chat_only_adjudicated: Anthropic return at 88ad6fe8 adjudicated; F1/F2 kept and R1/R2 closed with focused regressions and independent public replay.

## Bounded final stages SUCCESS_CONTRACT (2026-09-22; before edits)

Goal: a complete checked inventory larger than one final request can produce a
bounded answer and exact-answer independent review through normal advance,
without losing opposition, unused reasons, provenance, or honest failure.
Authority: the owner's accepted bounded final-writing/review commission at
`3d53e48702be0f90e31ff199292a33ea10b9ba16`, AGENTS.md, the semantic integration
contract, and the intelligence claim-support contract. Implementation is
explicitly authorized in `codex/bounded-final-stages`; no provider dispatch,
acquisition, publication, or finite-route adoption is authorized in this unit.

Invariants: preserve immutable original evidence and accepted jobs; every
checked finding and distinct unused/reused disposition reaches answer formation
and independent review; retain source-role/condition/version limits and stable
shared origin identities. No sampling, keyword filtering, silent truncation,
provider-size increase, automatic semantic success, or independent-vote fiction.
Existing compact rendering, normal job delivery and exact repair helpers remain
the integration boundaries. All generated requests include measured handoff.

Signals (PLANNED_NOT_OBSERVED at binding):

- Given an inventory exceeding one final request, question-wise bounded draft
  reconciliation and exact-answer review cover every checked record. Maximum
  permitted answer and review/correction context stay within each actual request
  envelope. A complete aggregate request or hidden growing accumulator is the
  forbidden near-miss; inspect every dispatched payload and measurement.
- Given opposing, complementary, duplicate-origin, version/condition-different,
  unused and mixed-source-role records split across batches, retain their exact
  membership and present their interaction to final actors. Seed omitted records,
  stale answer identities and unresolved material objections; require rejection
  at coverage/identity/final-status boundaries rather than an unrelated schema
  failure. Fixture responses prove plumbing only, not semantic quality.
- Given accepted jobs, repeat/resume preserves their bytes. Evidence, check and
  answer changes invalidate their actual downstream dependencies; legacy saved
  responses remain verifiable. Assert source jobs are not regenerated by checks.
- Given reopened originals or admissible exact repairs, every downstream request
  remains bounded and the final exact candidate receives complete evidence
  review. Conflicting, unclassified, oversized or unresolved repairs preserve
  the original answer and requires-revision status.

Wrong-cause baseline: the saved customer-only sizing fixture contains 60,901
rows, 8,020 meanings, 346 no-claim reason groups and 2,041 origins. Its permitted
7,994-token answer makes final review reserve 260,636 tokens against 258,400.
This is an offline structural failure, not a failed live semantic answer.
Parent independently replays this preserved fixture and owns live quality proof.

Review checkpoint: repository opt-in delegated-review policy applies; no review
is dispatched here. Return the changed boundary and independent-evidence gaps to
the parent for courier-only disposition. Current all-source materialization,
real-case semantic quality and execution allowance remain parent-owned.


## Bounded final stages implementation and validation (2026-09-22)

This unit extends the existing normal complete-case consumer. It preserves native
extraction/consolidation and the original evidence while making final writing,
independent review, reopening and correction bounded. Runtime proof below binds
`complete_case_consumer.py` SHA256
`3f62e865572b587875671deb28506d0b0743a4f526c39beeb3a22a76f62f2bd3`
and runner SHA256
`914ea8f1849272a815403c3ad165ca4bfaff7e733b0d9c7c66465cceec1948b6`.
The consumer hash is the validated Windows working-copy byte identity. Git
normalizes its CRLF line endings; the implementation commit
`7cb6a042c35b3546068e462b8650f23900744f51` stores LF blob SHA256
`5438506fa537b69156bb1f8375643c883c92095256da41cee252d2ab26e8611d`.
Fresh comparison found only that normalization, across all six committed files.
The live owning method is **Bounded complete-case answer and review** in the
semantic integration contract; the supported operating route points there.

Every question folds every checked batch into a bounded draft and reconciliation
ledger. Questions share one output allowance; cross-question composition
reconciles those drafts. Independent reviewers then receive the exact composed
answer against every checked batch, including uncited and unused/reused records.
The complete objection union stays local and lossless; each correction recheck
carries its own accepted assessment, nominations and exact edits. Actual delivery
measurement uses the same pure intake-envelope builder as normal job intake.
Oversized review contexts partition whole evidence/original/check components
with complete cross-product coverage; no final hidden all-record request remains.

The compiler supplies immutable source-role/origin identity for current and
cited handles. Model-authored duplicate role/origin ledger fields were removed.
Trusted phase instructions distinguish composition from subsequent independent
review and local assigned evidence from the complete inventory. Outside-batch
citation absence is not itself an objection or permission to reopen originals.
Existing exact text repairs already add their supporting source_refs to the
answer citations; this behavior is now explicit and tested, without widening
repair authority. Completed unchanged legacy outputs retain their prior route
and still revalidate actual requests, responses and immutable receipts. Legacy
reopening/correction now partitions an actually oversized downstream request.

### Structural checks and wrong-cause evidence

At implementation handoff, the consumer run used the edited worktree, not a copied export:
`python -m pytest tests/unit/test_complete_case_consumer.py -q -o addopts= --disable-warnings`
from `forseti-harness`: **56 passed in 66.19 seconds**.
Full stdout and command/cwd/hash/exit receipt:
`C:/Users/vmon7/AppData/Local/Temp/forseti-final-stages-implementation-g0d49hny/consumer-tests.txt`
and `consumer-tests.receipt.json` in the same directory. Both were freshly
read after completion. This includes public native CLI routing; every checked
record/unused disposition; exact final-answer review; shared question allowance;
maximum permitted answer/review/correction inputs; more than four local review
outputs; metadata and scope guards; correction citation addition; clean,
requires-revision and late-defect outcomes; restart and checks-only reuse; and
completed legacy response compatibility.

Two additional affected-boundary commands passed earlier in this worktree.
Their original tool outputs, rather than recreated stdout, are the receipts:

- `python -m pytest tests/unit/test_review_evidence.py tests/unit/test_finite_closeout_judgment.py tests/contract/test_data_lake_inventory_gate.py tests/contract/test_policy_module_version_pins.py -q --disable-warnings`:
  **68 passed**, tool output chunks `04dc28` / `12194c`. These shared helpers
  and contract boundaries did not change afterward.
- `python -m pytest tests/unit/test_semantic_evidence_integration.py -k 'judgment_jobs_complete or judgment_worker_delivery or judgment_job_rejects or judgment_job_retains or judgment_job_identity or advance_public_route or advance_invalid_response or advance_staged or advance_reuses_legacy' -q --disable-warnings`:
  **27 passed**, tool output chunks `2d554f` / `548a45`. The extracted runner
  envelope boundary did not change afterward.

Wrong-cause failures were observed before their fixes: a permitted 7,994-token
answer exceeded the historical final-review budget (260,636 versus 258,400);
completed legacy output reuse incorrectly requested new bounded jobs; and a
small checked inventory with twenty reopened original rows produced an actual
legacy review of 45,124 tokens against 40,000. Regression cases now reach their
intended final states without dropping evidence. A schema-valid outside-batch
reopen is specifically rejected at assigned-evidence admission. Scripted
responders establish structural behavior, not semantic quality.

Strict retrieval-header validation of the three edited documents and
`git diff --check` passed during implementation. Changed-file validation
wrappers run with `--base HEAD` found no committed changes and skipped:
those skips are not evidence for this uncommitted diff. Direct relevant contract
tests above provide their stated coverage; committed/CI gates remain closeout work.

### Independent archived fit and generated semantic failures

Parent-owned independent artifacts are under
`C:/Users/vmon7/AppData/Local/Temp/forseti-final-stages-validation-2d7d04274db4`.
The final `historical_fit_final.json` binds the runtime hash above and preserves
all 8,366 checked records (8,020 findings plus 346 reason groups) and all 8,020
role-origin relation edges. With the saved allowed 7,994-token answer, the final
compiler metadata produces three batches of 2,900, 2,816 and 2,650 records.
Actual reserved review totals are 234,550, 234,550 and 228,719 against 258,400;
margins are 23,850, 23,850 and 29,681. Their exact requests remain under
`historical-sizing-final/`, marked non-dispatchable. The earlier two-batch
`historical_fit.json` remains intermediate historical evidence only. The
additional metadata's third batch is the measured cost of preserving identity
after generated drafts demonstrated identity drift. This is offline historical
customer-only coverage/fit, not a current complete all-source case run.

Two parent-run generated synthetic probes failed and remain preserved:

- `initial_probe_failure.json`: a two-question fixture with a 2,000-token output
  reserve assigned 990 tokens to one question. The third schema-valid model
  response used 1,250 tokens and was rejected; two earlier drafts were accepted.
  Nothing was truncated, and final independent review was not reached.
- `production_reserve_probe_failure.json`: a later question fold remapped
  immutable handles; composition treated its intentional lack of checked rows
  as inability to perform independent review; a local reviewer reopened facts
  solely because they belonged to another batch. Another reviewer correctly
  found omitted citations but misunderstood existing repair citation addition.
  A third returned pass plus unresolved reopening and was rejected by the
  existing guard. No checked result was claimed. These observed semantic defects
  motivated compiler metadata and trusted phase-specific instructions.

The final pinned successor passed the parent's independent semantic evaluation:
`successor_semantic_evaluation.json` records **PASS_BOUNDED_STAGE_SEMANTICS**.
Six question-fold jobs, one cross-question composition and three independent
exact-answer reviews covered all twelve checked records and produced
`COMPLETE_CASE_ANSWER_CHECKED`, result SHA256
`c8de0abbc622b03c0d5675b33ac16dca0a9a4702a470e5dd6e35250413703404`.
All eight held-out meaning requirements passed: mixed current experience,
version/action scope, repeated origin, promotional roles, editorial observation
window, intent versus action, trend/price limits, and unverified discovery.
This exercised actual bounded stages with literal synthetic prechecked inputs;
it did not repeat source judgment or execute the current Summer Fridays case.
Public CLI routing has the separate structural proof above. The positive result
used no reopening or correction. `live-successor/restart_result.json` records
no new jobs and all 52 durable consumer JSON artifacts unchanged on restart.

`live-successor/negative/evaluation.json` records **PASS_SEMANTIC_NEGATIVE**:
the frozen wrong answer and reviews satisfied schema, identity and capacity,
but the first two batches detected unsupported universal comfort/96-hour repair,
old V1 defects promoted to V2, duplicate-origin independent repurchase, switching
intent promoted to action, and unsupported price/design actions. The last local
pass did not erase the seven material findings retained in the union. Exact
repair admission rejected proposed sources outside the open nominations' scope;
no corrected semantic-pass result was claimed. Separately,
`live-successor/identity-negative/evaluation.json` records a material semantic
defect when compiler metadata alone showed f0/f6 sharing origin 0, without a
literal repost hint. The reviewer did not request the outside-batch citation
or fail a structural guard. Same origin alone was not treated as proof of the
total number of purchase events.

`final_durable_test_audit.json` records 27 provider attempts and 25 accepted
outputs across these probes, including the two preserved admission failures
above, with zero automatic retries and all oracles withheld. Reported completed
provider usage was 805,726 input tokens (105,216 cached), 40,178 output tokens
and 17,607 reasoning-output tokens; these are reported usage categories, not a
savings or dollar-cost claim. All fourteen final successor requests, including
negative controls, were admitted within the declared 73,400-token test budget;
the maximum reserved envelope was 42,747. Earlier generated outputs were not
reused as proof for the changed instructions. The implementer did not read the
held-out oracle or make provider calls. Current Summer Fridays processing is
zero; its 20% execution allowance remains unused.

### Residuals and closeout boundary

At implementation handoff, structural tests, archived fit and the bounded
synthetic semantic evaluation passed. Independent structural review of oversized
original-context and correction partition coverage was still pending; its return
and adjudication are recorded below. The positive live semantic probe did not
exercise those branches. Current complete all-role
Summer Fridays materialization/execution remains unfinished and parent-owned.
An indivisible row/record, fixed context or cited-identity/answer context that
cannot fit still fails visibly. One original reinspection and one classified
exact repair do not guarantee resolution: ambiguous or failed reviews remain
requires-revision. Citation-only omissions without an admissible substantive
text edit also remain requires-revision. No arbitrary answer rewrite or repair
policy expansion was added.

The implementation touched the consumer, normal runner envelope helper, consumer
tests, owning semantic integration contract, supported route document, and this
report. No source evidence, saved historical output, provider configuration or
finite-route implementation changed. Historical sections above retain their
original authority and results; their prior closeout does not attest this change.

### Independent review and parent adjudication — 2026-09-22

The owner supplied an Anthropic Claude Opus 5 review return bound to clean
`9e82c2c9aba05133f3456946eb4e34eaa308e7ea`, with implementation commit
`7cb6a042c35b3546068e462b8650f23900744f51` verified as an ancestor. The reviewer
reported external direct write access and left exactly the consumer and its unit
tests changed. The parent observed that two-file diff before making further
changes. The reviewer could not independently prove exclusive ownership from
machine-wide process presence; that isolation limitation is not upgraded into a
stronger claim. The pasted return is the carrier, at
`C:/Users/vmon7/.codex/attachments/8399d80a-ccdc-48fb-8534-1db38a21c80b/Pasted text.txt`,
SHA256 `ecad5f6cfdee095890448b99d5e834db589bf16fed988a8eb39cb2f54fd8bb16`.
No separate review report was created.

The parent adjudicated the returned `CHANGES_PROPOSED` verdict and all four
reported issues:

- **F1, accept the returned patch.** Legacy original-source reopen packing now
  measures the complete worker delivery before choosing whole-row batches. The
  parent independently disabled only the returned sizing fix in memory: the
  reviewer's multi-row regression failed at 46,224 against a 40,000 fixture-byte
  capacity; retaining the patch passed. Whole rows and exact original coverage
  remain intact; an indivisible oversized row still fails.
- **Initial source packing, accept and close in the parent.** The same helper
  needs the actual root for initial reading too. A separate reproduction failed
  at 46,916 against 40,000 before the one-call-site fix. The regression proves
  fitting multi-row requests, exact 18-row coverage and accepted-source reuse.
- **Unfinished legacy restart, accept and close in the parent.** Completed-result
  compatibility alone left an already accepted assembly eligible for rewriting
  while its final review was unfinished. Exact bound saved assemblies now select
  their existing route, with normal response/receipt validation and bounded
  review of the actual saved answer. Missing, changed and staged response states
  fail visibly instead of switching routes. Independent fixtures seeded by the
  actual previous runtime at `3d53e48702be0f90e31ff199292a33ea10b9ba16` reproduced
  unnecessary question folds for 10, 11 and 12 rows before the fix. All three now
  complete without another assembly: one, one and two review jobs respectively,
  with every prior JSON byte unchanged and zero new jobs on repeat. The largest
  case's split reviews reserve 32,653 and 32,994 of 40,000 fixture bytes.
- **Failed-check scope precision, accept and close in the parent.** A unanimous
  known failure scope remains `upstream_only` or `answer`; mixed or unclassified
  scopes remain `unknown`. Passing batches cannot relabel a failure. Acceptance
  and exact-repair authority are unchanged.

Parent-added regression cases demonstrated **7 failures and 4 passes before**
the parent fixes, then **11 passes**. Final verification on the actual target
worktree passed **68 consumer tests**, **68 shared/contract tests** and **27
native intake/public-route tests**. Commands match the implementation checks
above, with `-o addopts=` to bind the local runs. Full stdout, the preserved
reviewer diff, reproduction scripts/results and hash-bound validation receipt are
under
`C:/Users/vmon7/AppData/Local/Temp/forseti-review-adjudication-09cfa01cd9d4/`.
The tested runtime byte SHA256 is
`359f1b2bcfd1f818abcb30585a1ff4a485de344f245fba6f71a6212af7bc7364`.
The runner remains byte-identical to the implementation's tested runner.

The reviewer also reported passing original/review cross-product and correction
partition probes. Their scratch paths were not supplied, so those observations
remain attributed reviewer evidence; they are not represented as parent-run
receipts. Parent whole-suite coverage retains the exact cross-product, late
objection, correction and restart regressions. This adjudication makes no new
live model calls. The prior synthetic semantic results remain evidence for the
unchanged phase instructions and identity representation, not a live proof of
reopening or a successfully corrected answer. Joint-context ambiguities and
incompatible repairs still fail closed; those residuals remain accepted.

All material review issues are closed in the retained patch. The transport-only
courier prompt `bounded_final_stages_review_patch_prompt_v0.md` is retired from
`docs/prompts/reviews/` in this same PR; its exact commission remains in Git at
`9e82c2c9aba05133f3456946eb4e34eaa308e7ea`. The contract now describes actual
whole-row delivery sizing, partial legacy resume and preserved known scopes.
The supported route already directs consumers to that contract. PR #1635 owns
this work unit; required CI and protected landing remain publication checks.
No current Summer Fridays processing or 20% execution allowance was consumed by
this adjudication. The next material move is the current all-source input
readiness check before its bounded run.

```text
review_routing_status: routed -- chat_only_adjudicated: Anthropic return at 9e82c2c9 adjudicated; F1 kept, initial packing and unfinished legacy restart fixed, known failure scope preserved; 163 targeted tests pass.
```
